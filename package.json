{
  "name": "filvert",
  "version": "0.0.1",
  "description": "A CLI tool for converting files format using TypeScript",
  "main": "index.js",
  "scripts": {
    "dev": "tsc -w",
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "filvert": "./dist/index.js"
  },
  "author": "Daniel Kalfa",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.12.8",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "commander": "^12.0.0"
  }
}